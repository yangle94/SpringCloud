spring:
  application:
    name: springbootAdmin
  cloud:
    config:
      discovery:
        enabled: true
        service-id: configserver
      fail-fast: true

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER_ADDRES}
