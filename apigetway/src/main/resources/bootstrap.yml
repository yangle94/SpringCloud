spring:
  application:
    name: apigetway
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config
      fail-fast: true
      #profile: dev
      label: release_20190830
      username: admin
      password: 123456

eureka:
  client:
    service-url:
      defaultZone: http://admin:123456@eureka-service:9001/eureka/
  instance:
    prefer-ip-address: true