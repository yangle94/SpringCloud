spring:
  application:
    name: apigetway
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config
      fail-fast: true

eureka:
  client:
    service-url:
      defaultZone: http://eureka-service:9001/eureka/
  instance:
    #服务间调用使用IP地址
    prefer-ip-address: true