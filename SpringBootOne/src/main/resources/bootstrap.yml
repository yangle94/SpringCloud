spring:
  application:
    name: springbootOne
  cloud:
    config:
      discovery:
        enabled: true
        service-id: configserver
#      profile: dev
eureka:
  client:
    service-url:
      defaultZone: http://netflix:9003/eureka/